1) git clone 'repositry_url.git' # clone remote repo into local machine 
2) git add .  # add all file to git enviroment
3) git commit -m 'some message' # save all changes
4) git push origin main # push changes to remote repo on mian branch
5) git branch  # show branchs
6) git checkout mian
7) git  checkout -b 'new branch'  # create new branch
8) git branch -M "new name"   # rename current branch
9) git branch -d 'branch name'  # delete branch
10) git diff "" 